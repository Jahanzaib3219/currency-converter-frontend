# Currency Converter Client (React + Vite + Bootstrap)

## Setup (Windows Command Prompt)
```cmd
cd client
copy .env.example .env
notepad .env
:: ensure VITE_API_BASE points to your server, e.g. http://localhost:5174
npm install
npm run dev
```
